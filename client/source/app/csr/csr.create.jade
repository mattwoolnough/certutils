.csr-add-view.full-width
    .card
        .card-header(class="p+")
            h3.title Create a new csr
        .card-content
            aio-csr-form(csr="vm.csr", submit="vm.addNewCsr", state="vm.state")
