.settings-add-view.full-width
    .card
        .card-header(class="p+")
            h3.title Create a new settings
        .card-content
            formly-form(model="vm.formData", fields="vm.formFields", settings="vm.settings", submit="vm.createNewsettings", state="vm.state")
            //aio-settings-form(settings="vm.settings", submit="vm.createNewsettings", state="vm.state")
